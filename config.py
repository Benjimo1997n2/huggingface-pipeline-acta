class Config:
    def __init__(self, name: str, model: str, tokenizer: str, model_name: str):
        self.name = name
        self.model = model
        self.tokenizer = tokenizer
        self.model_name = model_name
    